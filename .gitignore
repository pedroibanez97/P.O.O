# Material de origen que no es propio (infografías de @noveira.dev) y el PDF de referencia.
# La presentación no los necesita: todo su contenido es HTML/CSS propio.
*.jpg
*.jpeg
*.png
*.pdf
# ...menos el apunte del TP, que es propio y se comparte por link:
!apunte-tp-arena-de-combate.pdf
.DS_Store

# Entorno de Python para correr los juegos (no se sube al repo)
.venv/
__pycache__/
